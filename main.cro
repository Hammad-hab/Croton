<include "stdin"> # goddamn
<include "os">
fn Loop {
    inputln("croton% ") as name
    |if (is_equal(name "exit"))| as condition
    |condition then| as then
    then(fn {
        println("Exiting")
    }) as condition
    |condition else| as else
    else(fn {
        |os systeme(name)|
        Loop()
    })
}


Loop()
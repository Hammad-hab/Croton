<parserlog Loading-inputln.so....>
<parserlog Successfully-Loaded-inputln.so....>

define(USER_INPUT inputln(">>> "))
if(USER_INPUT) as condition

|condition then| as then

then(declare {
	println("User Entered " USER_INPUT)
}) as then_condition

|then_condition else| as else

else(declare {
	println("User did not enter anything, exiting program...")
})

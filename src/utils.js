const simulateStringFromParsedFunction = (token) => {
    let v = ""
    const str = token.name + "(" +  + ")"
    return str
}


module.exports = {
    simulateStringFromParsedFunction,
    CrotanFunction
}